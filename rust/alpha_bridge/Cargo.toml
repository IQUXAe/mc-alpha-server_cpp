[package]
name = "alpha_bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"

[dependencies]
libc = "0.2"
flate2 = "1"
zstd = "0.13"
